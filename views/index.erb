<div class="pure-g">
  <!-- This div is just here as an offset to center the next div. It's a hack. I feel bad about it. -->
  <div class="pure-u-1-3">&nbsp;</div>

  <div class="pure-u-1-3">
    <h1> Jif Jenerator </h1>
    <form action="/search" method="get" >
      <!-- What form inputs will you need for this to work? Put them here -->
      <input name='confirm' type='text'>

      <input type='submit'>
    </form>

    <!-- Maybe show an error message here if the HTTP call didn't work? -->

    <% if @gif %>
<img src="<%= @gif['images']['fixed_height']['url'] %>">
<% else %>
  <h2> bye felicia </h2>
    <% end %>
  </div>
</div>